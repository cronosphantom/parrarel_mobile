import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parallellapp.mobile',
  appName: 'Parallell',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
